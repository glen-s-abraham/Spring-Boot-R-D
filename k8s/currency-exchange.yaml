apiVersion: apps/v1
kind: Deployment
metadata:
  name: currency-exchange
spec:
  replicas: 1
  selector:
    matchLabels:
      app: currency-exchange
  template:
    metadata:
      labels:
        app: currency-exchange
    spec:
      containers:
        - name: currency-exchange
          image: iss211/currency-exchange:0.0.1-SNAPSHOT
          ports:
            - containerPort: 8000
          env:
            - name: SPRING_CONFIG_IMPORT
              value: "optional:configserver:http://config-server:8888"
            - name: EUREKA_CLIENT_SERVICEURL_DEFAULTZONE
              value: "http://naming-server:8761/eureka"
---
apiVersion: v1
kind: Service
metadata:
  name: currency-exchange
spec:
  selector:
    app: currency-exchange
  ports:
    - protocol: TCP
      port: 8000
      targetPort: 8000
  type: ClusterIP
